[pytest]
addopts = -q
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    smoke        : fast, critical checks
    regression   : broader end-to-end flows
    auth         : login/permission tests
    admin        : django admin page checks
console_output_style = progress
