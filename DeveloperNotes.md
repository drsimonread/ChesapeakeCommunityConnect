# Research


## Topic : Google Maps page not Loading correctly- Camilla Galeano
The issue is that our map is giving us a a google pop up that says "This page can't load Google Maps correctly." I believe that the issue is due to API keys, I have enabled Maps JavaScript API. I am still get a pop up, I think it has something to do with billing, but doesn't change the output. I have changed the mapPage.html to have the updated API key for the maps.